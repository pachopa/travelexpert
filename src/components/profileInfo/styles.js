import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  root: {
    maxWidth: "50%",
  },
  userName: {
    fontSize: 20,
    fontWeight: 700,
  },
  colBorder: {
    border: "solid",
    borderColor: "#b8daff",
    borderRadius: 4,
  },
}));
